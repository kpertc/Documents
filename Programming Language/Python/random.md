#python 

```Python
# random between two value
random.uniform(0, 10) # float and uniform distrubution
random.randrange(0, 10) # int

# random choice
mylist = ["apple", "banana", "cherry"]
random.choice(mylist)

random.gauss(mu, sigma)
# mu -> mean
# sigma -> standard deviation
```